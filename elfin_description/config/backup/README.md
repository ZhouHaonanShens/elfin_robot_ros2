# Backup Configuration Files

This directory contains configuration files that are not currently in use but may be needed in the future.

## Files:

- `gz_ros2_control_plugin.xml` - Plugin alias configuration for gz_ros2_control
  - Created during Jazzy migration to work around plugin loading issues
  - May be useful if Gazebo plugin loading mechanism changes in future versions
  - Original location: elfin_description/config/